# rajasekar-work-site

Edit resume content in `resume-data.json`, then run:

```bash
npm run build
```

Vercel runs the same build command on deploy, so pushing JSON changes is enough
for the generated site files to update during deployment.
